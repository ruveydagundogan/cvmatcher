services:
  - type: web
    name: cvmatcher-api
    runtime: go
    buildCommand: cd backend && go build -tags netgo -ldflags '-s -w' -o app ./cmd/server
    startCommand: cd backend && ./app
    envVars:
      - key: GO_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: DB_HOST
        fromDatabase:
          name: cvmatcher-db
          property: host
      - key: DB_PORT
        fromDatabase:
          name: cvmatcher-db
          property: port
      - key: DB_USER
        fromDatabase:
          name: cvmatcher-db
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: cvmatcher-db
          property: password
      - key: DB_NAME
        fromDatabase:
          name: cvmatcher-db
          property: database
      - key: DB_SSLMODE
        value: require
      - key: CORS_ALLOWED_ORIGINS
        value: "*"
      - key: MLC_LLM_ENABLED
        value: "true"
      - key: MLC_LLM_BASE_URL
        value: "/tunnel"
    healthCheckPath: /health/live
    autoDeploy: true

databases:
  - name: cvmatcher-db
    plan: free
    databaseName: llmdecisionscore
    user: llmdecisionscore
